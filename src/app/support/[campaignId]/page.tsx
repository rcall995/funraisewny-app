'use client';

// This is a temporary, simplified version for debugging purposes.
export default function SupportPage() {
  return (
    <div className="min-h-screen bg-gray-50 flex flex-col items-center p-8">
      <div className="w-full max-w-2xl text-center">
        <h1 className="text-3xl font-bold">Support Page Test</h1>
        <p className="mt-4">If you are seeing this on the live site, the build was successful.</p>
      </div>
    </div>
  );
}